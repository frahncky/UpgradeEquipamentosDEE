# Modelos de ofícios por empresa

Esta pasta contém **38 modelos LaTeX personalizados** para prospecção de doações e parcerias para o DEE/IFMA.

## Compilação no Overleaf

Cada arquivo numerado (`01_...tex` a `38_...tex`) é um documento LaTeX completo e pode ser selecionado diretamente como **Main document** no Overleaf.

Passos:
1. Abra **Menu** no Overleaf;
2. Em **Main document**, selecione o arquivo da empresa desejada;
3. Use compilador **pdfLaTeX**;
4. Clique em **Recompile**.

O arquivo `base_empresa.tex` contém apenas o corpo comum dos ofícios e não deve ser escolhido como documento principal.

## Novos modelos adicionados

- `31_fnirsi.tex` — FNIRSI;
- `32_sel.tex` — Schweitzer Engineering Laboratories (SEL);
- `33_rigol.tex` — RIGOL Technologies;
- `34_siglent.tex` — SIGLENT Technologies;
- `35_endress_hauser.tex` — Endress+Hauser;
- `36_universal_robots.tex` — Universal Robots;
- `37_fanuc.tex` — FANUC;
- `38_kuka.tex` — KUKA.

Todos os modelos usam o padrão visual definido em `modelos/preambulo.tex` e os dados institucionais de contato do Chefe do DEE são inseridos pelo modelo-base.
